import { initAllNewsCarousels } from './news-carousel.js';

// Initialize news carousel when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    console.log('Initializing news carousels...');
  initAllNewsCarousels();
});
