{
  "name": "lab-iuh",
  "private": true,
  "version": "0.0.26",
  "type": "module",
  "scripts": {
    "agents:sync": "./scripts/sync-agent-links.sh",
    "dev": "vite",
    "build": "vite build",
    "test": "node --test",
    "preview": "vite preview",
    "version:patch": "yarn version patch",
    "version:minor": "yarn version minor",
    "version:major": "yarn version major",
    "build:patch": "yarn version:patch && yarn build",
    "build:minor": "yarn version:minor && yarn build",
    "build:major": "yarn version:major && yarn build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "glob": "^11.0.0",
    "postcss": "^8.4.49",
    "sass": "^1.97.2",
    "svgo": "^3.3.2",
    "tailwindcss": "^3.4.17",
    "vite": "^7.2.4",
    "vite-plugin-svgo": "^1.0.5"
  },
  "dependencies": {
    "swiper": "^12.2.0"
  }
}
